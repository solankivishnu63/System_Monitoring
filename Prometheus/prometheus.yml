
# ==========================================================
# Prometheus Main Configuration
# For Linux Monitoring (Prometheus + Node Exporter + Grafana)
# ==========================================================

global:
  scrape_interval: 15s     # How often to scrape targets
  evaluation_interval: 15s # How often to evaluate alert rules

  external_labels:
    monitor: "linux-monitoring"

# ======= Scrape Configurations =======

scrape_configs:
  # --------------------------
  # Prometheus Self Monitoring
  # --------------------------
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # --------------------------
  # Node Exporter Monitoring
  # --------------------------
  - job_name: "node_exporter"
    static_configs:
      - targets:
          - "localhost:9100"   # Local system
          # Add more servers like:
          # - "192.168.1.10:9100"
          # - "server2.example.com:9100"

# ------------------------------
# OPTIONAL: Alerting (if needed)
# ------------------------------
# Uncomment this section if using Alertmanager
#
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets:
#             - "localhost:9093"
#
# rule_files:
#   - "/etc/prometheus/alert-rules.yml"
